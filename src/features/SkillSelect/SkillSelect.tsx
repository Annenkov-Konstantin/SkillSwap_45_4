import { DropdownTrigger } from "@/shared/ui"

type SkillSelectProps = {} 

export const SkillSelect: React.FC<SkillSelectProps> = ({

}) => {
  return (<DropdownTrigger isOpen={true} onClick={() =>{}}></DropdownTrigger>)
}